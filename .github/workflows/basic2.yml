name: basic2

on:
  label:
    types:
      - created
  issue_comment:
    types:
      - created
    labels:
      - bug
  push:
    branches:
      - feature/*
  workflow_dispatch:
  repository_dispatch:
    types: [build]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - run: ls
